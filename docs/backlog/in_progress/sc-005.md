# [SC-005] IN PROGRESS - Refactor WSL Manager command dispatch (DRY)

**Status**: In Progress
**Priority**: Medium
**Component**: `tools/pslib/wsl/lib/commands.ps1`, `tools/pslib/wsl/lib/manager.ps1`

**Summary**:
As a WSL manager user, I want action commands to prompt me for missing arguments so that I don't have to remember the exact CLI syntax to use a command. As a developer, I want a single dispatch path so that CLI and interactive menu don't duplicate routing logic.

**Description**:
This item covers two related refactorings:

### Phase 1 — Self-validating action functions (done)
The `Invoke-WslManager` switch block inconsistently handled missing arguments. Some commands passed arguments through, some branched to `*Interactive` wrappers, and one hard-failed with `exit 1`. Each action function now owns its argument validation: accept what's given, prompt for what's missing. The `*Interactive` wrapper functions were merged into their non-Interactive counterparts.

### Phase 2 — DRY command dispatch
`Invoke-WslManager` (CLI entry) and `Show-InteractiveMenu` (TUI entry) both contain switch blocks that map commands to action functions — violating DRY. Extract a single `Invoke-WslCommand` dispatcher that both call.

**Current duplication**:
- `Invoke-WslManager` switch: maps command strings → action function calls with CLI parameters
- `Show-InteractiveMenu` switch: maps menu keys → action function calls with pre-fetched `$Distros`
- Both live in `commands.ps1` alongside the action functions they dispatch to

**Scope decisions**:
- Extract `Invoke-WslCommand` dispatcher in `commands.ps1` — single switch block mapping command names to action functions
- Move `Invoke-WslManager` and `Show-InteractiveMenu` to new `manager.ps1` — orchestration separate from action logic
- `Invoke-WslManager` calls `Invoke-WslCommand` after parsing CLI args
- `Show-InteractiveMenu` maps menu keys to command names, wraps `Invoke-WslCommand` in a single try/catch instead of per-command try/catch boilerplate
- Display helpers (`Format-DistroListEntry`, `Show-WslDistroList`) stay in `commands.ps1` since action functions use them
- `wsl-manager.ps1` updated to source both `commands.ps1` and `manager.ps1`
- Integration tests updated to source `manager.ps1`

**Acceptance Criteria**:

Phase 1 (done):
- [x] All `IsNullOrWhiteSpace` checks removed from `Invoke-WslManager` switch
- [x] Each action function prompts for missing required arguments
- [x] `Invoke-SetupDockerInteractive` merged into `Invoke-SetupDocker` and removed
- [x] `Invoke-SetupPodmanInteractive` merged into `Invoke-SetupPodman` and removed
- [x] `Invoke-SetupProxyInteractive` merged into `Invoke-SetupProxy` and removed
- [x] `Invoke-SetupUserInteractive` merged into `Invoke-SetupUser` and removed
- [x] `Invoke-RepairInterop` created with distro name prompting when not provided
- [x] `repair-interop` no longer hard-fails with `exit 1`

Phase 2:
- [x] `Invoke-WslCommand` dispatcher created in `commands.ps1`
- [x] `Invoke-WslManager` and `Show-InteractiveMenu` moved to `manager.ps1`
- [x] `Invoke-WslManager` delegates to `Invoke-WslCommand`
- [x] `Show-InteractiveMenu` delegates to `Invoke-WslCommand` (single try/catch, not per-command)
- [x] `wsl-manager.ps1` sources `manager.ps1`
- [x] Integration tests source `manager.ps1`
- [x] Unit tests updated/split for new file structure
- [x] All existing tests continue to pass
